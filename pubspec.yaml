name: flutter_neumorphic_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_neumorphic: ^3.1.0
  flutter_colorpicker: ^0.4.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  pedantic: ^1.11.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
  fonts:
    - family: Samsung
      fonts:
        - asset: fonts/SamsungSans-Bold.ttf
